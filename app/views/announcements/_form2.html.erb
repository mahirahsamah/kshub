
<%= form_with(model: announcement) do |form| %>
  <% if announcement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(announcement.errors.count, "error") %> prohibited this announcement from being saved:</h2>

      <ul>
        <% announcement.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>here</p>
  <%= @etitle %>
  <h1> Does the Announcement for this Event look OK? </h1>
  <div class="card" style="border-color:#913031; margin:20vh; margin-top:50px;">
  <div class="card-body">
  
  
  <div class="field">
    <%= form.label :title, { class: "form-label"} %>
    <%= form.text_field :title, :value => params[:etitle], class: "form-control"   %>
  </div>

  <div class="field">
    <%= form.label :body, { class: "form-label"} %>
    <%= form.text_area :body, :value => @ebody, class: "form-control" %>
  </div>


      <div class="actions" style="margin-top:20px">
        <div style="display:flex;">
          <div style="width:33%; margin-left: 0vw"> 
            <%= link_to 'Back', announcements_path, class: "btn btn-outline-secondary newMerchandise"  %>
          </div>
          <div style="width:33%; justify-content:center; display:flex"> 
            <%= form.submit "Submit", class: "btn btn-outline-secondary newMerchandise"  %>
          </div>
          <div style="width:33%"> </div>


        </div>

      </div>
      
    </div>
</div>
<% end %>
