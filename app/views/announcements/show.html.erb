
<p>
  <strong>Title:</strong>
  <%= @announcement.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @announcement.body %>
</p>

<p>
  <strong>User:</strong>
  <%= @announcement.user.username %>
</p>

<% if current_user && current_user.admin %>
<%= link_to 'Edit', edit_announcement_path(@announcement) %> |
<% end %>
<%= link_to 'Back', announcements_path %>


<%= link_to("Add Comment", new_comment_path(annoucement_id: @announcement.id)) %>
<%#<%= link_to("Edit Comment", edit_comment_path(announcement_id: @announcement.id, comment_id: @comment.id))%>

