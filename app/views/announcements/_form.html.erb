<%= form_with([@user, @user.announcement.build]) do |form| %>
  <% if announcement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(announcement.errors.count, "error") %> prohibited this announcement from being saved:</h2>

      <ul>
        <% announcement.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :AnnouncementID %>
    <%= form.text_field :AnnouncementID %>
  </div>

  <div class="field">
    <%= form.label :Text %>
    <%= form.text_area :Text %>
  </div>

  <div class="field">
    <%= form.label :UIN %>
    <%= form.text_field :UIN %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
